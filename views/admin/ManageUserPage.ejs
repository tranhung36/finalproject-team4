<%- include("./partialsAdmin/sidebar") %>
<div class="container col-xl-10 crud-page">
    
    <div class="table-control">
        <a href="" class="btn btn-primary mb-2" style="float: right">Thêm mới</a>
        <table class="table table-striped table-hover table-bordered paginated">
            <thead>
                <tr>
                    <th scope="col">STT</th>
                    <th scope="col">First name</th>
                    <th scope="col">Last name</th>
                    <th scope="col">Gmail</th>
                </tr>
            </thead>
            <% userInfo.forEach((user,index)=> {%>
                <tbody>
                    <tr>
                        <td><%= index + 1 %></th>
                        <td><%= user.first_name %></td>
                        <td><%= user.last_name %></td>
                        <td><%= user.email %></td>
                        <td>
                        <a class="btn btn-primary" href="">Update</a>
                        <a class="btn btn-info delete-product" href="">Delete</a>
                        </td>
                    </tr>
                </tbody>
                <% }) %>
        </table>
    </div>
</div>
